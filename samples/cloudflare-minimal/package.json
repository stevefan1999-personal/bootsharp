{
  "name": "bootsharp-cloudflare-minimal",
  "private": true,
  "type": "module",
  "scripts": {
    "publish:cs": "dotnet publish backend/Cloudflare.Minimal.csproj -c Release /p:BsLlvm=true /p:MSBuildEnableWorkloadResolver=false",
    "dev": "npm run publish:cs && wrangler dev",
    "deploy": "npm run publish:cs && wrangler deploy",
    "types": "wrangler types",
    "check:ts": "npm run types && tsc --noEmit -p tsconfig.json",
    "size": "wrangler check startup"
  },
  "devDependencies": {
    "wrangler": "^4.122.0",
    "typescript": "^5.9.3"
  }
}
