{
  "name": "bootsharp-cloudflare-razor",
  "private": true,
  "type": "module",
  "scripts": {
    "publish:cs": "dotnet publish backend/Cloudflare.Razor.csproj -c Release /p:BsLlvm=true /p:MSBuildEnableWorkloadResolver=false",
    "dev": "npm run publish:cs && wrangler dev",
    "deploy": "npm run publish:cs && wrangler deploy",
    "types": "wrangler types",
    "migrate:local": "wrangler d1 migrations apply bootsharp-cf-razor --local",
    "migrate:remote": "wrangler d1 migrations apply bootsharp-cf-razor --remote",
    "check:ts": "npm run types && tsc --noEmit -p tsconfig.json",
    "size": "wrangler check startup"
  },
  "devDependencies": {
    "wrangler": "^4.122.0",
    "typescript": "^5.9.3"
  }
}
