{
  "name": "bootsharp-do-interleave",
  "private": true,
  "type": "module",
  "scripts": {
    "publish:cs": "bash ../../scripts/interleave-test.sh --publish-only",
    "check:ts": "wrangler types && tsc --noEmit -p tsconfig.json",
    "dev": "wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "^4.122.0",
    "typescript": "^5.9.3"
  }
}
