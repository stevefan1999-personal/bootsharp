<Project>
  <!-- The NativeAOT-LLVM toolchain hack, shared by every lane so a host toolchain move fixes in one
       place. See ../emscripten.props for why the pack version is never written down. -->
  <Import Project="../emscripten.props"/>
</Project>
