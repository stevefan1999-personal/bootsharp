{
  "name": "bootsharp-signalr",
  "private": true,
  "type": "module",
  "scripts": {
    "publish:cs": "bash ../../scripts/signalr-test.sh --publish-only",
    "check:ts": "wrangler types && tsc --noEmit -p tsconfig.json",
    "dev": "wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "^4.122.0",
    "typescript": "^5.9.3",
    "@microsoft/signalr": "^8.0.7",
    "ws": "^8.21.0"
  }
}
